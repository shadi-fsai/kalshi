"""Streamlit UI package for the Kalshi Kelly app.

Holds the shared, Streamlit-bound building blocks (cached data fetchers, the
sidebar settings, the bet sizer, the games browser, and the portfolio view) that
the multipage app in ``pages/`` composes. Pure market/finance logic continues to
live in the ``kalshi`` package.
"""
